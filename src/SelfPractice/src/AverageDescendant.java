public class AverageDescendant {

	public static void main(String[] args) {

//		return sol(root, 0, 0);
	}


	/*
	check if each node in tree are average of all of its descendent nodes
	 */
	public boolean sol(TreeNode root, int[] currSum, int nodes) {

		if(root == null){
			return true;
		}
//		currSum[0] = sol(root.left, currSum, nodes) + sol(root.left, currSum, nodes)

	return false;
	}


}
